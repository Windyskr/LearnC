// 小蓝已经玩了一会儿（不一定按最优方案玩），他想知道，对于他目前的局面，最少还需要多少步可以到达目标。
#include <algorithm>
#include <cstring>
#include <iostream>
using namespace std;
const int N = 190;
int a[N], b[N], c[N];

// 汉诺塔游戏
int main() {
    int a, b, c, n;
    cin >> a >> b >> c;

    return 0;
}
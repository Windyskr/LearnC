#include <stdio.h>
#include <math.h>
int main()
{
    float x=2 , y=2, ix, iy, rx, ry;
    printf ("请输入坐标(x,y):");
    scanf ("(%f,%f)", &ix, &iy);
    rx = abs(ix);
    ry = abs(iy);
    if (rx < )
    {
        
    }
    else
    {

    }

}